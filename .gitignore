# Deployment artifacts, written on the host, never in a checkout.
deploy/source.env
run/
rollback/
var/
.guarded-run

# Guard manifests ARE history: a claim without the manifest that records it
# is a claim.  Browser screenshots are machine-and-moment artifacts, and
# worktree-fingerprint excludes evidence/ either way.
evidence/browser/

__pycache__/
*.pyc
.incoming/
.DS_Store
